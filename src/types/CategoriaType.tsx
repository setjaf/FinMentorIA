export interface CategoriaType {
    id?: number | string // Identifador de la categoria al pricipio será un string
    nombre: string // Nombre con visible para el usuario de la PWA
    color: string // Color de la categoria, se usará para mostrar en la UI
}